[project]
name = "voicebot"
version = "0.1.0"
description = "Telegram voice-to-voice bot with ElevenLabs"
requires-python = ">=3.11"
dependencies = [
  "aiogram>=3.4.1",
  "httpx>=0.27.0",
  "python-dotenv>=1.0.1",
]

[tool.uv]
package = false
